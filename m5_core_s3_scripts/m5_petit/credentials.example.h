// Copy this file to credentials.h and fill in real values / このファイルを credentials.h にコピーして実際の値を入れてください

// Wi-Fi settings (priority order: falls back from ssid1 to ssid2) / WiFi設定（優先順位順：ssid1→ssid2 にフォールバック）
// If using at home only, put the same value in both ssid1 and ssid2 / 家でのみ使うときはssid1もssid2も同じものをいれてください
const char* ssid1= "<HOME_SSID>";             // home Wi-Fi router / 家のWiFiルーター
const char* pass1= "<HOME_PASSWORD>";
const char* ssid2= "<TRAVEL_SSID>";   // travel router e.g. GL.iNET (optional) / GL.iNET等の旅行用ルーター（任意）
const char* pass2= "<TRAVEL_PASSWORD>";


